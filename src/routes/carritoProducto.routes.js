import { Router } from "express";
import { createCategoria, getCategories } from "../controllers/categorias.controller.js";

const router = Router();

/* router.post("/carrito", createCategoria)

router.get("/categories", getCategories) */


export default router;